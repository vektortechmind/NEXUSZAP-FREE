{
  "name": "nexus-chatbot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n api,web -c green,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "test": "npm run test --prefix backend && npm run test:chat-frontend --prefix frontend",
    "test:smoke": "npm run test --prefix backend"
  },
  "dependencies": {
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "tsx": "^4.22.3"
  },
  "license": "SEE LICENSE IN LICENSE.md"
}
