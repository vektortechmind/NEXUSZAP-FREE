/**
 * Config explícita no frontend para o postcss-load-config não subir ao package.json
 * da raiz do monorepo (evita falha se esse JSON estiver inválido).
 * Estilos Tailwind: @tailwindcss/vite em vite.config.ts.
 */
export default {}
